# 🚀 데이터 로딩 속도 개선 가이드

## 🔍 문제 진단

### 느린 로딩 및 새로고침 필요한 이유

1. **브라우저 캐싱**: 브라우저가 API 응답을 캐시하여 오래된 데이터를 보여줌
2. **axios 캐싱**: axios 기본 설정으로 인한 캐싱
3. **프론트엔드 상태 갱신 없음**: useEffect가 한 번만 실행되어 변경사항 반영 안 됨
4. **JPA/Hibernate 캐싱**: 백엔드에서 엔티티 캐싱으로 인한 지연

---

## ✅ 적용된 해결책

### 1. 프론트엔드 캐시 방지 설정

**파일**: `frontend/src/demo/services/apiService.js`

```javascript
// axios 기본 설정: 캐시 방지
axios.defaults.headers.common['Cache-Control'] = 'no-cache, no-store, must-revalidate';
axios.defaults.headers.common['Pragma'] = 'no-cache';
axios.defaults.headers.common['Expires'] = '0';

// axios 인터셉터: 모든 GET 요청에 타임스탬프 추가
axios.interceptors.request.use((config) => {
  if (config.method === 'get') {
    config.params = {
      ...config.params,
      _t: Date.now() // 타임스탬프 추가하여 브라우저 캐시 방지
    };
  }
  return config;
});
```

### 2. 백엔드 캐시 방지 헤더 추가

**파일**: `src/main/java/com/example/choprest/controller/RestaurantController.java`

```java
// 모든 응답에 캐시 방지 헤더 추가
private HttpHeaders getNoCacheHeaders() {
    HttpHeaders headers = new HttpHeaders();
    headers.set("Cache-Control", "no-cache, no-store, must-revalidate");
    headers.set("Pragma", "no-cache");
    headers.set("Expires", "0");
    return headers;
}

// 모든 응답에 적용
return ResponseEntity.ok()
    .headers(getNoCacheHeaders())
    .body(restaurants);
```

### 3. 자동 데이터 새로고침 기능

**파일**: `frontend/src/pages/main/HomePage.js`, `NearMePage.js`

```javascript
// 페이지 포커스 시 자동 새로고침
useEffect(() => {
  const handleVisibilityChange = () => {
    if (!document.hidden) {
      fetchData(); // 페이지가 다시 보일 때 데이터 새로고침
    }
  };

  const handleFocus = () => {
    fetchData(); // 창에 포커스가 돌아올 때 데이터 새로고침
  };

  document.addEventListener('visibilitychange', handleVisibilityChange);
  window.addEventListener('focus', handleFocus);

  return () => {
    document.removeEventListener('visibilitychange', handleVisibilityChange);
    window.removeEventListener('focus', handleFocus);
  };
}, []);
```

---

## 🎯 개선 효과

### 이전
- ❌ 브라우저 캐시로 인한 오래된 데이터 표시
- ❌ 수동 새로고침 필요
- ❌ 변경사항이 즉시 반영되지 않음

### 개선 후
- ✅ 항상 최신 데이터 표시
- ✅ 자동 새로고침 (페이지 포커스 시)
- ✅ 변경사항 즉시 반영
- ✅ 캐시 없이 항상 서버에서 최신 데이터 가져오기

---

## 🔧 추가 최적화 방법

### 1. 데이터베이스 쿼리 최적화

**인덱스 추가** (MySQL):
```sql
CREATE INDEX idx_restaurant_name ON restaurants(restaurant_name);
CREATE INDEX idx_region_name ON restaurants(region_name);
CREATE INDEX idx_category ON restaurants(category);
```

### 2. 프론트엔드 최적화

**React Query 사용** (선택사항):
- 자동 캐싱 및 갱신
- 백그라운드 데이터 동기화
- 더 나은 성능

### 3. 백엔드 최적화

**JPA 쿼리 최적화**:
- `@Query` 어노테이션으로 최적화된 쿼리 사용
- 페이징 처리 (대량 데이터)

---

## 📊 성능 모니터링

### 확인 방법

1. **브라우저 개발자 도구**
   - Network 탭에서 응답 헤더 확인
   - `Cache-Control: no-cache` 확인
   - 요청 URL에 타임스탬프(`_t=...`) 확인

2. **백엔드 로그**
   - API 호출 시간 확인
   - 데이터베이스 쿼리 시간 확인

---

## ⚠️ 주의사항

1. **타임스탬프 추가로 인한 URL 변경**
   - 모든 GET 요청에 `_t=...` 파라미터 자동 추가
   - 브라우저 캐시를 완전히 우회

2. **자동 새로고침 빈도**
   - 페이지 포커스 시에만 새로고침
   - 너무 자주 새로고침하지 않음

3. **성능 영향**
   - 캐시를 사용하지 않으므로 서버 부하 증가 가능
   - 하지만 항상 최신 데이터 보장

---

## 💡 추가 팁

### 수동 새로고침 버튼 추가 (선택사항)

```javascript
const handleRefresh = () => {
  fetchData();
};

// 새로고침 버튼 추가
<button onClick={handleRefresh}>새로고침</button>
```

### 데이터 갱신 간격 조절

```javascript
// 5분마다 자동 새로고침 (선택사항)
useEffect(() => {
  const interval = setInterval(() => {
    fetchData();
  }, 5 * 60 * 1000); // 5분

  return () => clearInterval(interval);
}, []);
```

---

## ✅ 체크리스트

- [x] axios 캐시 방지 설정 추가
- [x] 백엔드 캐시 방지 헤더 추가
- [x] 자동 새로고침 기능 추가
- [x] 타임스탬프 기반 캐시 우회
- [ ] 데이터베이스 인덱스 최적화 (선택)
- [ ] React Query 도입 (선택)

---

## 🎉 결과

이제 데이터가 항상 최신 상태로 표시되고, 새로고침 없이도 변경사항이 자동으로 반영됩니다!

