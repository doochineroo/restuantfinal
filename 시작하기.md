# 🚀 ChopPlan 프로젝트 시작하기

## 📋 준비 사항

필수 설치:
- ✅ **Java 17+** (`java -version`으로 확인)
- ✅ **Node.js** (`node -v`로 확인)
- ✅ **MySQL** (XAMPP 또는 직접 설치)

---

## 🗂️ 1단계: MySQL 데이터베이스 설정

### 방법 A: 자동 설정 (권장)
```bash
setup-local-database.bat
```

이 스크립트가:
- MySQL 설치 확인
- 데이터베이스 `chopplan` 생성
- 연결 테스트

### 방법 B: 수동 설정

1. **MySQL 실행 확인**
   - XAMPP 사용 시: Control Panel에서 MySQL "Start"
   - 또는 서비스 관리자에서 MySQL 서비스 시작

2. **데이터베이스 생성**
   ```bash
   mysql -u root -p
   # 비밀번호 입력 (없으면 Enter만)
   ```
   
   ```sql
   CREATE DATABASE IF NOT EXISTS `chopplan` CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
   EXIT;
   ```

3. **비밀번호 확인/설정**
   - `src/main/resources/application.properties` 파일 확인
   - 현재 설정: 비밀번호 `1234`
   - MySQL 비밀번호가 다르면 수정:
     ```properties
     spring.datasource.password=실제비밀번호
     ```

---

## ⚙️ 2단계: 백엔드 실행

### 방법 A: 빠른 시작 스크립트 (권장)
```bash
quick-test-local.bat
```

이 스크립트가:
- 백엔드 빌드
- 서버 시작 (포트 8080)

### 방법 B: 수동 실행
```bash
# 빌드
gradlew.bat clean build

# 실행
gradlew.bat bootRun
```

**백엔드 실행 확인:**
- 브라우저에서 http://localhost:8080/api/restaurants 접속
- 데이터가 보이면 성공!

---

## 🎨 3단계: 프론트엔드 실행

**새 터미널 창을 열어서** 실행하세요!

### 방법 A: npm 직접 실행
```bash
cd frontend
npm install    # 처음 한 번만
npm start
```

### 방법 B: 스크립트 실행 (있는 경우)
```bash
cd frontend
START_FRONTEND.bat
```

**프론트엔드 실행 확인:**
- 자동으로 브라우저가 열리면 성공!
- 또는 http://localhost:3000 접속

---

## 🌐 접속 주소

실행 후 접속 가능한 주소:

- **프론트엔드**: http://localhost:3000
- **백엔드 API**: http://localhost:8080/api
- **API 테스트**: http://localhost:8080/api/restaurants

---

## ✅ 체크리스트

시작 전 확인사항:
- [ ] MySQL 실행 중
- [ ] 데이터베이스 `chopplan` 생성됨
- [ ] `application.properties` 비밀번호 확인
- [ ] 백엔드 실행 성공 (포트 8080)
- [ ] 프론트엔드 실행 성공 (포트 3000)

---

## ❓ 문제 해결

### "Connection refused" 오류
- MySQL 서비스가 실행 중인지 확인
- XAMPP: Control Panel에서 MySQL 시작
- 포트 3306이 사용 가능한지 확인

### "Access denied" 오류
- `application.properties`의 비밀번호 확인
- MySQL root 비밀번호 확인:
  ```bash
  mysql -u root -p
  ```

### "Unknown database 'chopplan'" 오류
- 데이터베이스가 생성되었는지 확인
- 다시 생성:
  ```sql
  CREATE DATABASE `chopplan` CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
  ```

### 포트 충돌
- **8080 포트**: 다른 Java 애플리케이션이 실행 중일 수 있음
  ```bash
  kill-port-8080.bat
  ```
- **3000 포트**: 다른 React 앱이 실행 중일 수 있음

---

## 💡 팁

1. **두 터미널 사용**
   - 터미널 1: 백엔드 실행 (`quick-test-local.bat`)
   - 터미널 2: 프론트엔드 실행 (`cd frontend && npm start`)

2. **데이터베이스 확인**
   ```bash
   mysql -u root -p
   USE chopplan;
   SHOW TABLES;
   ```

3. **API 테스트**
   ```bash
   curl http://localhost:8080/api/restaurants
   ```

---

## 🎯 다음 단계

로컬 테스트 완료 후:
- **GCP 배포**: `quick-deploy.bat` 실행
- 또는 [GCP_COMPLETE_DEPLOYMENT_GUIDE.md](GCP_COMPLETE_DEPLOYMENT_GUIDE.md) 참고

