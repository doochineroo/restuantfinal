# 데이터베이스에서 데이터 가져오는 순서

## 전체 흐름

### 1. 프론트엔드 (React) 요청
```
HomePage 진입
  ↓
React Query 훅 호출
  - useAllRestaurants()
  - usePopularRestaurants(10)
  - useRecentPopularRestaurants(6)
  ↓
API 호출
  - GET /api/restaurants/all
  - GET /api/statistics/popular-restaurants-with-count?limit=10
  - GET /api/statistics/recent-popular-restaurants?limit=6
```

### 2. 백엔드 (Spring Boot) 처리 순서

#### A. 모든 식당 조회 (`/api/restaurants/all`)
```
RestaurantController.getAllRestaurants()
  ↓
RestaurantService.getAllRestaurants()
  ↓
1. JPA 1차 캐시 클리어 (entityManager.clear())
   → 항상 최신 데이터 조회 보장
  ↓
2. 데이터베이스 조회 (restaurantRepository.findAll())
   → MySQL: SELECT * FROM restaurants
  ↓
3. 필터링 (filterOperatingRestaurants)
   → 위치 정보가 있는 식당만 선택 (latitude, longitude 있음)
  ↓
4. 리뷰 개수와 평점 설정 (setReviewCountsAndRatings)
   → 배치 쿼리로 N+1 문제 방지
   → MySQL: 
     - SELECT restaurant_id, COUNT(*) FROM reviews GROUP BY restaurant_id
     - SELECT restaurant_id, AVG(rating) FROM reviews GROUP BY restaurant_id
  ↓
5. JSON 응답 반환
```

#### B. 인기 식당 조회 (`/api/statistics/popular-restaurants-with-count`)
```
StatisticsController.getPopularRestaurantsWithCount()
  ↓
StatisticsService.getPopularRestaurantsWithCount()
  ↓
1. 클릭 통계 조회
   → MySQL: SELECT restaurant_id, COUNT(*) as click_count 
            FROM click_statistics 
            GROUP BY restaurant_id 
            ORDER BY click_count DESC 
            LIMIT 10
  ↓
2. 식당 정보 조회 (restaurantRepository.findAllById())
   → MySQL: SELECT * FROM restaurants WHERE id IN (...)
  ↓
3. 리뷰 개수와 평점 설정 (setReviewCountsAndRatings)
   → 배치 쿼리
  ↓
4. JSON 응답 반환
```

### 3. 리뷰 개수/평점 설정 상세 (setReviewCountsAndRatings)
```java
// 1단계: 모든 식당 ID 추출
List<Long> restaurantIds = [1, 2, 3, ...]

// 2단계: 배치로 리뷰 개수 조회 (N+1 문제 방지)
SELECT restaurant_id, COUNT(*) as count 
FROM reviews 
WHERE restaurant_id IN (1, 2, 3, ...)
GROUP BY restaurant_id
→ 결과: [(1, 5), (2, 10), (3, 0), ...]

// 3단계: 배치로 평점 조회
SELECT restaurant_id, AVG(rating) as avg_rating 
FROM reviews 
WHERE restaurant_id IN (1, 2, 3, ...)
GROUP BY restaurant_id
→ 결과: [(1, 4.5), (2, 4.8), (3, 0.0), ...]

// 4단계: 각 식당 객체에 설정
restaurant.setReviewCount(5);
restaurant.setRating(4.5);
```

## 데이터 로딩 타임라인

### 초기 로드 (페이지 진입 시)
```
0ms    - React 컴포넌트 마운트
        ↓
10ms   - React Query 훅 실행
        ↓
20ms   - API 요청 시작 (3개 동시)
        ↓
50ms   - 백엔드 요청 수신
        ↓
100ms  - 데이터베이스 쿼리 실행
        ↓
150ms  - 리뷰 개수/평점 배치 쿼리 실행
        ↓
200ms  - JSON 응답 생성
        ↓
250ms  - 프론트엔드 응답 수신
        ↓
300ms  - React Query 캐시 저장
        ↓
350ms  - 화면 렌더링 완료
```

### 캐시 사용 시 (30초 이내 재방문)
```
0ms    - React 컴포넌트 마운트
        ↓
10ms   - React Query 훅 실행
        ↓
20ms   - 캐시 확인 → 데이터 즉시 반환
        ↓
30ms   - 화면 렌더링 완료 (API 호출 없음!)
```

## 데이터베이스 쿼리 순서

### 1단계: 식당 기본 정보
```sql
SELECT 
  id, restaurant_name, branch_name, region_name,
  latitude, longitude, road_address, phone_number,
  category, opening_hours, ...
FROM restaurants
WHERE latitude IS NOT NULL 
  AND longitude IS NOT NULL
```

### 2단계: 리뷰 통계 (배치)
```sql
-- 리뷰 개수
SELECT restaurant_id, COUNT(*) as review_count
FROM reviews
WHERE restaurant_id IN (?, ?, ?, ...)
GROUP BY restaurant_id

-- 평균 평점
SELECT restaurant_id, AVG(rating) as avg_rating
FROM reviews
WHERE restaurant_id IN (?, ?, ?, ...)
GROUP BY restaurant_id
```

### 3단계: 클릭 통계 (인기 식당용)
```sql
SELECT restaurant_id, COUNT(*) as click_count
FROM click_statistics
GROUP BY restaurant_id
ORDER BY click_count DESC
LIMIT 10
```

## 최적화 포인트

1. **JPA 1차 캐시 클리어**: `entityManager.clear()`로 최신 데이터 보장
2. **배치 쿼리**: N+1 문제 방지 (식당마다 개별 쿼리 X)
3. **React Query 캐시**: 30초 동안 캐시 유지하여 재방문 시 즉시 표시
4. **병렬 처리**: 여러 API를 동시에 호출하여 속도 향상

## 성능 개선 효과

### 이전 (N+1 문제)
- 식당 100개 조회 시:
  - 식당 조회: 1번
  - 리뷰 개수: 100번 (각 식당마다)
  - 평점: 100번 (각 식당마다)
  - 총 쿼리: 201번

### 현재 (배치 쿼리)
- 식당 100개 조회 시:
  - 식당 조회: 1번
  - 리뷰 개수: 1번 (배치)
  - 평점: 1번 (배치)
  - 총 쿼리: 3번

## React Query 캐시 전략

```javascript
staleTime: 30초
  → 30초 동안은 캐시된 데이터 사용
  → API 호출 없이 즉시 표시

refetchOnMount: true
  → 첫 마운트 시에는 항상 최신 데이터 가져오기

refetchOnWindowFocus: false
  → 포커스 시 갱신 안 함 (너무 자주 호출 방지)

gcTime: 10분
  → 10분 동안 캐시 유지
  → 메모리에서 삭제되지 않음
```

